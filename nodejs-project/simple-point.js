class Point {
  constructor() {
    // fix the constructor such that the constructor accepts an x and y
    // argument and sets instance properties `this.x` and `this.y` to those
    // arguments.
    // for example, `new Point(4, 10)` should result in `this.x = 4` and `this.y = 10`.
  }
}

// { autofold
module.exports = {
  Point
};
// }
