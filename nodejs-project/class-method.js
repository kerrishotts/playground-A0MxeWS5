class Rect {
  constructor(w, h) {
    this.w = w;
    this.h = h;
  }

  // create a method named calculateArea that returns the area of the rectangle.
  // if you don't remember the formula for area, it's width multipled by height.

  // also, create a method named calculatePerimeter that returns the perimeter
  // of the rectangle. If you don't remember the formula, it's twice the height
  // and twice the width.
}

// { autofold
module.exports = {
  Rect
};
// }
